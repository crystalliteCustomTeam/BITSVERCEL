if ($(window).width() < 992) {
     $('.project-prerequisite-slider').slick({
          slidesToShow: 1,
          dots: true,
          arrows: false,
          adaptiveHeight: true,
          autoplay: true,
          autoplaySpeed: 7000,
     });

}